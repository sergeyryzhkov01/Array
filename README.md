## 📋 Лабораторная работа №11  
### Тема: Работа с одномерными массивами

Задание:
1. Ввести массив целых чисел с клавиатуры.
2. Найти минимальный элемент массива.
3. Определить индекс минимального элемента.
4. Определить количество элементов, расположенных до минимального.

Результат:
- массив вводится с консоли;
- минимальный элемент определяется перебором;
- выводится значение минимума и его позиция.

---

## 📋 Лабораторная работа №14  
### Тема: Передача массивов в функции

Задание:
1. Организовать ввод массива в отдельной функции.
2. Реализовать функцию поиска минимального элемента.
3. Реализовать функцию вывода массива.
4. Использовать передачу массива по указателю.

Результат:
- все операции с массивом вынесены в функции;
- массив передаётся в функции по указателю;
- программа имеет структурированный вид.

---

## 📋 Лабораторная работа №16  
### Тема: Динамические массивы

Задание:

1. Реализовать заполнение массивов случайными вещественными числами.
2. Создать новый массив, состоящий:
   - из элементов с чётными индексами массива A;
   - из элементов с нечётными индексами массива B.
3. Использовать динамическое выделение памяти.

Результат:
- программа разбита на модули;
- используются динамические массивы;
- реализована работа с несколькими массивами.

---

## 📁 Структура проекта

```text
lab-arrays/
├── src/
│   ├── arrays.h   // объявления функций
│   ├── arrays.c   // реализация функций
│   └── app.c      // точка входа в программу
├── Makefile
└── README.md
```
***Реализованные функции*** 
1. **Для лабораторных №11 и №14:**
 - int input_array(int* arr, int n) — ввод массива
 - int print_array_int(const int* arr, int n) — вывод массива
 - int index_of_min(const int* arr, int n) — поиск индекса минимума

2. **Для лабораторной №16**
- double* full_elements_random(double* arr, int size) — заполнение массива случайными числами
- int print_array_double(const double* arr, int size) — вывод массива
- double* mix_arrays(...) — формирование нового массива
